{
  "name": "fungi-hackaton",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=24"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:client": "npm run dev --workspace client",
    "dev:client:silent": "npm run dev:noopen --workspace client",
    "build:client": "npm run build --workspace client",
    "dev:server": "npm run dev --workspace server",
    "start:server": "npm run start --workspace server"
  },
  "dependencies": {
    "express": "^4.19.2",
    "nvm": "^0.0.4",
    "socket.io": "^4.8.3"
  }
}