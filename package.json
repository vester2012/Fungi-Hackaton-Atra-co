{
  "name": "fungi-hackaton",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:client": "npm run dev --workspace client",
    "dev:client:open": "npm run dev:open --workspace client",
    "build:client": "npm run build --workspace client",
    "dev:server": "npm run dev --workspace server",
    "start:server": "npm run start --workspace server",
    "start": "npm run dev:client:open"
  },
  "dependencies": {
    "socket.io": "^4.8.3"
  }
}
